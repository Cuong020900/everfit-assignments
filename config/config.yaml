app:
  port: 3000
  cors:
    origins:
      - 'http://localhost:3000'

database:
  host: localhost
  port: 5432
  name: workout_db
  synchronize: false
  logging: false
  migrations:
    - dist/database/migrations/*.js

logging:
  level: info
